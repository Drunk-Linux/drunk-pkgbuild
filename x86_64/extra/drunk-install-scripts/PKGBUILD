# Description: Utilities to administer Access Control Lists, which are used to define more fine-grained discretionary access rights for files and directories
# URL:         http://savannah.nongnu.org/projects/acl
# Maintainer:  Emmett1, emmett1 dot 2miligrams at gmail dot com
# Depends on:  

pkgname=drunk-install-scripts
pkgver=24
pkgrel=1
arch=('x86_64')
source=(http://server01.netkas.me:3000/Drunk-Distro/$pkgname/archive/master.tar.gz)

build() {
        cd $pkgname
        make
}

package() {
	cd $pkgname
	make DESTDIR=$pkgdir install
}
