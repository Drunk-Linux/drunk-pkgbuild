pkgname=xfwm4
pkgver=4.16.1
pkgrel=1
arch=('x86_64')
depends=(libwnck libxfce4ui libxinerama)
source=(https://archive.xfce.org/src/xfce/$pkgname/4.16/$pkgname-$pkgver.tar.bz2)

build() {
	cd $pkgname-$pkgver

	./configure		\
	--prefix=/usr		\
	--sysconfdir=/etc LIBS=-lX11

	make
}

package() {
	cd $pkgname-$pkgver

	make DESTDIR=$pkgdir install
}
