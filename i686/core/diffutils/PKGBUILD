pkgname=diffutils
pkgver=3.8
pkgrel=1
arch=('i686')
depends=(glibc)
makedepends=(gcc)
source=(https://ftp.gnu.org/gnu/diffutils/diffutils-$pkgver.tar.xz)

build() {
	cd $pkgname-$pkgver

	./configure		\
	--prefix=/usr

	make
}

package() {
	cd $pkgname-$pkgver

	make DESTDIR=$pkgdir install
}
